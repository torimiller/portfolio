import React, { Fragment } from 'react';
import { connect } from 'react-redux';

const About = () => {
    return (
        <Fragment>
            <h1>About Page</h1>
            
        </Fragment>
    )
}

export default connect(null)(About);